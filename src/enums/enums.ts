export enum ApiEnum {
  API_V1 = '/api/v1',
  API_V2 = '/api/v2'
}

export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc'
}

export enum RouteEnum {
  DETAIL = '/detail',
  COLLECTIONS = '/collections' // TOOD: need to change
}
